/* @module universe-graph
   The published entry point for the standalone graph page. The implementation
   lives in assets/universe/ (the same components the reader uses; graph-page
   is the thin shell). This file exists so the page URL for the script never
   changes. */
import './universe/graph-page.js';
