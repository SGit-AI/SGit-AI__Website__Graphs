/* @module book-files
   The published entry point for a book's file explorer. Same shell as the
   document explorer (assets/explorer/shell.js); the book-specific data views
   are in assets/explorer/bookview.js. A separate entry point rather than a
   shared one, so each section's pages name their own script. */
import './explorer/shell.js';
