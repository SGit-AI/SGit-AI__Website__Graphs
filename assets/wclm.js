/* @module wclm
   The published entry point for the WCLM experiment (brief 31). The
   implementation lives in assets/wclm/ (engine: the pure deterministic
   transformer; wclm-page: the shell). This file exists so the page URL for
   the script never changes. */
import './wclm/wclm-page.js';
