/* @module universe-view
   The published entry point for the universe reader. The implementation lives
   in assets/universe/ (core: pure logic; components: custom elements; reader:
   the shell). This file exists so the page URL for the script never changes. */
import './universe/reader.js';
