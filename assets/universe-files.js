/* @module universe-files
   The published entry point for the document file explorer. The implementation
   lives in assets/explorer/ (shell) and assets/universe/core/fileview.js (the
   pure colorizers and document views). This file exists so the page URL for the
   script never changes. */
import './explorer/shell.js';
