/* @module universe-files
   The published entry point for the document file explorer. The implementation
   lives in assets/universe/ (core/fileview: the pure colorizers and views;
   files-page: the shell). This file exists so the page URL for the script
   never changes. */
import './universe/files-page.js';
