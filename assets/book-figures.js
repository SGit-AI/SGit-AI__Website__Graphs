/* @module book-figures
   The published entry point for a book's figure viewer. The implementation is
   in assets/figures/ (core/figureview: the pure renderers; shell: the filters
   and the one-figure view). This file exists so the page URL for the script
   never changes. */
import './figures/shell.js';
