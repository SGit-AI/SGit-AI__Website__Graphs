/* @module book-board
   The published entry point for a book's project board. The implementation is
   in assets/board/ (core/board: the pure renderers by schema; shell: the tab
   strip, the fetch and the drill-down). This file exists so the page URL for
   the script never changes. */
import './board/shell.js';
