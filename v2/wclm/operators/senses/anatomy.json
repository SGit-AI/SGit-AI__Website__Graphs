{
 "operator": "senses",
 "note": "The code's own extraction (brief 37): authored segments, anchored by the exact text of each first line; the build resolves heads to line ranges and fails on drift.",
 "segments": [
  {
   "id": "doc",
   "kind": "docs",
   "head": "/* @module wclm/operators/senses",
   "label": "the module's claim",
   "does": "Two clues per word: its NUMBER (plural means more than one involved, proved by the stem families) and its ACTIVE SENSE (the document's own unless the caller switches it — a switched word is foreign and withdraws downstream).",
   "feeds": [
    "contract"
   ],
   "lines": [
    1,
    6
   ]
  },
  {
   "id": "setup",
   "kind": "imports",
   "head": "'use strict';",
   "label": "the pure senses helpers",
   "does": "numberOf and senseOf from the shared senses module — the stem-family number proof and the register lookup (a plural inherits its singular's senses through the family).",
   "vars": {
    "numberOf": "{num, base, family} or null — the number evidence",
    "senseOf": "the word's active sense + its options, or null when the register holds nothing"
   },
   "feeds": [
    "run"
   ],
   "lines": [
    7,
    9
   ]
  },
  {
   "id": "contract",
   "kind": "contract",
   "head": "export const block = {",
   "label": "the pipeline contract",
   "does": "Optional; reads stream, writes stream — an annotating engine, one of the two that can mark words for withdrawal.",
   "feeds": [
    "run"
   ],
   "lines": [
    10,
    13
   ]
  },
  {
   "id": "run",
   "kind": "step",
   "head": "  run(s, world) {",
   "label": "the annotation pass",
   "does": "For each known non-padding token: attach number evidence if the family proves a pair; look the word up in the senses register (directly, or through its singular base); attach the ACTIVE sense — the caller's choice from opts.senses if valid, else 'doc'. A non-doc choice marks the token foreign. senseTable collects one row per WORD (not per token) for the page's picker.",
   "vars": {
    "chosen": "opts.senses — {word: senseKey} picked in the UI",
    "nm": "the number evidence",
    "sn": "the active sense record",
    "t.foreign": "true — this word now speaks another world's language",
    "s.senseTable": "one row per word: the picker's data"
   },
   "reads": [
    "s.resolved",
    "world.stems",
    "world.senses",
    "s.opts.senses"
   ],
   "writes": [
    "s.resolved — num/sense/foreign marks",
    "s.senseTable"
   ],
   "feeds": [
    "ui"
   ],
   "lines": [
    14,
    31
   ]
  },
  {
   "id": "ui",
   "kind": "export",
   "head": "export const ui = {",
   "label": "the workbench declaration",
   "does": "Prompts with sense-bearing words, plus presets that switch graph to a chart and node to a lymph node.",
   "lines": [
    32,
    38
   ]
  }
 ]
}
