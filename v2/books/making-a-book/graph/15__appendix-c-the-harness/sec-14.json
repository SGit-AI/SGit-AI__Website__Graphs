{"sec": "sec:Reading the release table as data", "blocks": [{"id": "blk:Reading the release table as data/1", "kind": "para", "range": [8186, 8378], "text": "The release notes live in HTML tables across three pages. Parsing them into\nversion | date | note triples makes them greppable, and the chronology in Appendix B is\ngenerated from the result:", "sentences": [{"n": 1, "text": "The release notes live in HTML tables across three pages.", "words": ["The", "release", "notes", "live", "in", "HTML", "tables", "across", "three", "pages"]}, {"n": 2, "text": "Parsing them into\nversion | date | note triples makes them greppable, and the chronology in Appendix B is\ngenerated from the result:", "words": ["Parsing", "them", "into", "version", "date", "note", "triples", "makes", "them", "greppable", "and", "the", "chronology", "in", "Appendix", "B", "is", "generated", "from", "the", "result"]}], "spans": [{"id": "mk:Reading the release table as data/1.1", "kind": "code", "covers": ["wrd:Reading the release table as data/1.2.4", "wrd:Reading the release table as data/1.2.5", "wrd:Reading the release table as data/1.2.6"]}], "uid": "mab-15:b32"}, {"id": "blk:Reading the release table as data/2", "kind": "code", "range": [8380, 9285], "text": "```python\nimport re, sys\nfrom html.parser import HTMLParser\n\nclass T(HTMLParser):\n    def __init__(s):\n        super().__init__(); s.rows=[]; s.cur=None; s.cell=None\n    def handle_starttag(s, t, a):\n        if t == 'tr': s.cur = []\n        elif t in ('td', 'th') and s.cur is not None: s.cell = []\n        elif t == 'br' and s.cell is not None: s.cell.append(' ')\n    def handle_endtag(s, t):\n        if t == 'tr' and s.cur is not None:\n            if s.cur: s.rows.append(s.cur)\n            s.cur = None\n        elif t in ('td', 'th') and s.cell is not None:\n            s.cur.append(re.sub(r'\\s+', ' ', ''.join(s.cell)).strip()); s.cell = None\n    def handle_data(s, d):\n        if s.cell is not None: s.cell.append(d)\n\np = T(); p.feed(open(sys.argv[1], encoding='utf-8').read())\nfor r in p.rows:\n    if len(r) >= 3 and re.match(r'v?\\d+\\.\\d+\\.\\d+', r[0]):\n        print(f\"{r[0]} | {r[1]} | {r[2]}\")\n```", "uid": "mab-15:b33"}, {"id": "blk:Reading the release table as data/3", "kind": "rule", "range": [9287, 9290], "text": "---", "uid": "mab-15:b34"}]}
